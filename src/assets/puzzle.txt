aoc::puzzle!("%puzzle%");

impl Solution for Puzzle {
    type Input = ();

    fn parse(input: &str) -> Self::Input {}

    fn solve(input: Self::Input) -> Option<String> {
        Some("".into())
    }
}

