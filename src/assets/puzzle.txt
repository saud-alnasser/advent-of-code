aoc::puzzle!("%puzzle%");

impl Solution for Puzzle {
    type Input = ();

    fn parse(input: &str) -> Self::Input {}

    fn solve_part1(structure: Self::Input) -> Option<String> {
        Some("".into())
    }

    fn solve_part2(structure: Self::Input) -> Option<String> {
        Some("".into())
    }
}
